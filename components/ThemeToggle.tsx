"use client"

/**
 * ThemeToggle is disabled because the application strictly follows
 * the user's device / operating system theme preference (prefers-color-scheme)
 * without manual theme overrides.
 */
export function ThemeToggle({ className }: { className?: string }) {
  return null
}
